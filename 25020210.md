# 25020210 — Fix Call Back CallerId

## Scop

Înlocuire callerId din client cu `TWILIO_CALLER_ID` și normalizare `clientNumber` la E.164.

## Fișiere modificate

- server/lib/normalize-phone.js
- server/whatsapp-integration-v6-index-correct.js

## Ce am făcut

- `normalizeToE164` pentru `clientNumber` (libphonenumber-js)
- Folosit `process.env.TWILIO_CALLER_ID` (fallback `+40373805828`)
- Setat `statusCallback` la `/api/voice/callback/status`
- Log: `Using Twilio Caller ID` și `Twilio payload`
- Salvat `callerIdUsed` în Database
- Recomandări: idempotent‑create, endpoint separat pentru `statusCallback`, validare `X‑Twilio‑Signature`
