/**
 * Shared config for provisioning scripts.
 * Single source of truth for admin email; provisioning may refuse admin for others unless --force.
 */
export const ADMIN_EMAIL = 'ursache.andrei1995@gmail.com';
